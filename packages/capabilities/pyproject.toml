[project]
name = "pil-capabilities"
version = "0.1.0"
description = "The closed capability vocabulary -- what a product may do to an external system through a connection."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pil_capabilities"]
