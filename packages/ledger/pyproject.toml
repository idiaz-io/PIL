[project]
name = "pil-ledger"
version = "0.1.0"
description = "Seal and verify hash-chained evidence. Library only; no store, no KMS."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pil_ledger"]
